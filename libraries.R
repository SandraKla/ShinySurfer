if("colourpicker" %in% rownames(installed.packages())){
  library(colourpicker, warn.conflicts = FALSE)} else{
    install.packages("colourpicker")}

if("cowplot" %in% rownames(installed.packages())){
  library(cowplot, warn.conflicts = FALSE)} else{
    install.packages("cowplot")}

if("plyr" %in% rownames(installed.packages())){
  library(plyr, warn.conflicts = FALSE)} else{
    install.packages("plyr")}

if("dplyr" %in% rownames(installed.packages())){
  library(dplyr, warn.conflicts = FALSE)} else{
    install.packages("dplyr")}

if("DT" %in% rownames(installed.packages())){
  library(DT, warn.conflicts = FALSE)} else{
    install.packages("DT")}

if("ggplot2" %in% rownames(installed.packages())){
  library(ggplot2, warn.conflicts = FALSE)} else{
    install.packages("ggplot2")}

if("glmnet" %in% rownames(installed.packages())){
  library(glmnet, warn.conflicts = FALSE)} else{
    install.packages("glmnet")}

if("grid" %in% rownames(installed.packages())){
  library(grid, warn.conflicts = FALSE)} else{
    install.packages("grid")}

if("gridExtra" %in% rownames(installed.packages())){
  library(gridExtra, warn.conflicts = FALSE)} else{
    install.packages("gridExtra")}

if("plotly" %in% rownames(installed.packages())){
  library(plotly, warn.conflicts = FALSE)} else{
    install.packages("plotly")}

if("processx" %in% rownames(installed.packages())){
  library(processx, warn.conflicts = FALSE)} else{
    install.packages("processx")}

if("readxl" %in% rownames(installed.packages())){
  library(readxl, warn.conflicts = FALSE)} else{
    install.packages("readxl")}

if("reshape2" %in% rownames(installed.packages())){
  library(reshape2, warn.conflicts = FALSE)} else{
    install.packages("reshape2")}

if("scales" %in% rownames(installed.packages())){
  library(scales, warn.conflicts = FALSE)} else{
    install.packages("scales")}

if("shiny" %in% rownames(installed.packages())){
  library(shiny, warn.conflicts = FALSE)} else{
    install.packages("shiny")}

if("shinyWidgets" %in% rownames(installed.packages())){
  library(shinyWidgets, warn.conflicts = FALSE)} else{
    install.packages("shinyWidgets")}

if("shinydashboard" %in% rownames(installed.packages())){
  library(shinydashboard, warn.conflicts = FALSE)} else{
    install.packages("shinydashboard")}

if("tidyr" %in% rownames(installed.packages())){
  library(tidyr, warn.conflicts = FALSE)} else{
    install.packages("tidyr")}

if("tidyverse" %in% rownames(installed.packages())){
  library(tidyverse, warn.conflicts = FALSE)} else{
    install.packages("tidyverse")}